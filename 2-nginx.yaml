- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: install naginix
    ansible.builtin.apt:
      name: nginx
      state: present
  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes  

